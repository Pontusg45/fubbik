/** A named value lookup.
 * @param name - The lookup name.
 * @returns The matching value.
 * @example
 * ```ts
 * find('hello')
 * ```
 */
export function find(name: string): string {
    return name;
}
